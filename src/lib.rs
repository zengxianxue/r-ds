mod hash_table;
mod heap;
mod stack;
mod queue;
mod array;
mod linked_list;
mod tree;
mod graph;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
